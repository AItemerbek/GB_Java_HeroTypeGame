package units;

public class Melee extends HeroBase{

    public Melee(String name, int maxHealth, int health, int armor, int damage, double criticalChance, double evasion, int x, int y) {
        super(name, maxHealth, health, armor, damage, criticalChance, evasion, x, y);
    }
}
